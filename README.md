# 🔐 Алгоритм шифрування Цезаря мовою C++

## 📌 Опис проєкту

Цей проєкт реалізує класичний алгоритм шифрування Цезаря мовою програмування C++.  
Програма дозволяє зашифрувати введений текст та розшифрувати його за тим самим ключем (зсувом).

Шифрування працює лише з латинськими літерами (A-Z, a-z).  
Інші символи (пробіли, цифри, розділові знаки) залишаються без змін.

---

## 🧠 Принцип роботи

Алгоритм Цезаря — це метод шифрування, при якому кожна літера зміщується на певну кількість позицій в алфавіті.

Наприклад, при зсуві 3:

A → D  
B → E  
C → F  

У програмі:
- використовується англійський алфавіт (26 літер);
- підтримуються великі та малі літери;
- зсув може бути додатнім або від’ємним;
- використовується операція modulo для коректної циклічності.

---

## ⚙️ Основні функції

### Шифрування
```cpp
string caesarEncrypt(const string text, int shift);
